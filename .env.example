# Local development: a file path SQLite uses directly.
# Production: a hosted libSQL URL, e.g. "libsql://your-db.turso.io"
DATABASE_URL="file:./db/local.db"

# Only required when DATABASE_URL points at a hosted libSQL/Turso database.
# Leave unset for local file-based development.
DATABASE_AUTH_TOKEN=
